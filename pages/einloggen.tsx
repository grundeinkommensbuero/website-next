import { ReactElement } from 'react';

const Login = (): ReactElement => {
  return (
    <div className="pageWidth">
      <h1 className="my-16">login goes here :)</h1>
    </div>
  );
};

export default Login;
